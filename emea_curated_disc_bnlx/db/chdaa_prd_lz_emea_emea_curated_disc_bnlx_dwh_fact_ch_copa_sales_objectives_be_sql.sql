CREATE TABLE chdaa_prd_lz_emea.emea_curated_disc_bnlx.dwh_fact_ch_copa_sales_objectives_be_sql (
  ETLLastModifiedDate TIMESTAMP,
  ETLChecksum STRING,
  dsbe STRING,
  ck_disc_act DECIMAL(14,2),
  ck_gross_qnty_act DECIMAL(17,3),
  d_pasite STRING,
  `0cust_grp5` STRING,
  `0division_edit` STRING,
  drespo STRING,
  `0cust_group___t` STRING,
  `0fiscyear` STRING,
  `0price_grp` STRING,
  `0coorder` STRING,
  `0pstng_date` STRING,
  rk_fc1_quantity DECIMAL(17,3),
  `0gl_account` STRING,
  dcustgrp3___t STRING,
  dprodh4 STRING,
  ETLLineageID STRING,
  `0calquart1` STRING,
  `0distr_chan_edit` STRING,
  `0salesorg___t` STRING,
  `0country_shipto` STRING,
  ck_disc_pln DECIMAL(14,2),
  ck_net_sales_amt_pln DECIMAL(17,2),
  ck_not_assigned_act DECIMAL(14,2),
  `0country___t_billto` STRING,
  dlp_form STRING,
  product_category STRING,
  `0country___t_soldto` STRING,
  d_cs_grpa STRING,
  d_raube STRING,
  `0cust_grp1` STRING,
  ck_rebates_other_act DECIMAL(14,2),
  d_gtin1 STRING,
  `0country___t_payer` STRING,
  `0customer___t_shipto` STRING,
  `0customer_shipto_trim` STRING,
  cluster STRING,
  dcgrp STRING,
  product_iri_ean_code STRING,
  `0cust_grp2` STRING,
  dpc STRING,
  `0company` STRING,
  `0curkey_lc` STRING,
  `0division___t` STRING,
  `0cs_chart` STRING,
  `0customer___t_payer` STRING,
  `0fiscper3` STRING,
  `0customer_shipto` STRING,
  `0street_billto` STRING,
  customer_country_code STRING,
  customer_status STRING,
  product_iri_material_name STRING,
  ck_rebates_act DECIMAL(14,2),
  customer_main_sap_customer_code_1 STRING,
  product_brand STRING,
  `0accnt_asgn_1` STRING,
  `0bill_type` STRING,
  dbrand STRING,
  dprodh2___t STRING,
  `0cs_doctype` STRING,
  d_gtin2u___t STRING,
  rk_quantity_pln DECIMAL(17,3),
  `0company___t` STRING,
  dcustgrp1 STRING,
  `0country_billto` STRING,
  d_fgpsu STRING,
  d_uvp STRING,
  d_xnetsal STRING,
  d_gtin1c STRING,
  customer_employee_name STRING,
  objective_eur INT,
  `0country_soldto` STRING,
  year DATE,
  ck_disc_cash_act DECIMAL(14,2),
  dprodh5 STRING,
  `0calyear` STRING,
  ck_discnt_amt_act DECIMAL(17,2),
  d_gtin2c STRING,
  period_mtd_pmtd INT,
  `0postal_cd_shipto` STRING,
  dcustgrp1___t STRING,
  `0version` STRING,
  `0country___t_shipto` STRING,
  `0cust_sales___t` STRING,
  `0customer_payer` STRING,
  dprodh5___t STRING,
  `0city_payer` STRING,
  d_cpckd STRING,
  dprodh1___t STRING,
  date_1 DATE,
  dcustgrp2 STRING,
  customer_sap_customer_name STRING,
  `0cust_group` STRING,
  `0extmatlgrp` STRING,
  ck_rebates_other_pln DECIMAL(14,2),
  `0distr_chan___t` STRING,
  `0prodh6` STRING,
  ck_returns_act DECIMAL(14,2),
  d_gtin2u STRING,
  product_status STRING,
  ck_secondary_rebates_act DECIMAL(17,2),
  rls_auth_sid STRING,
  `0curkey_gc` STRING,
  `0distr_chan` STRING,
  ck_gross_qnty_pln DECIMAL(17,3),
  dan_cog STRING,
  dprodh1 STRING,
  period_ytd_pytd_full_month INT,
  rk_fc2_quantity DECIMAL(17,3),
  ck_returns_pln DECIMAL(14,2),
  rk_quantity DECIMAL(17,3),
  `0matl_type___t` STRING,
  dvf STRING,
  product_name STRING,
  rk_fc3_amount DECIMAL(17,2),
  `0matl_group` STRING,
  dprodh6 STRING,
  period_lfy_plfy INT,
  `0cust_sales` STRING,
  d_gtin1u___t STRING,
  d_raube___t STRING,
  d_xgross STRING,
  `0accnt_asgn` STRING,
  ck_disc_cash_pln DECIMAL(14,2),
  objective_type STRING,
  d_cbpck STRING,
  dcustgrp4___t STRING,
  ck_net_sales_amt_act DECIMAL(17,2),
  `0material` STRING,
  `0doc_type` STRING,
  `0plant` STRING,
  d_gtin1c___t STRING,
  dcustgrp5 STRING,
  `0cs_item` STRING,
  division___t STRING,
  ck_gross_sales_amt_pln DECIMAL(17,2),
  main_sap_material_code STRING,
  d_msrca STRING,
  dbg STRING,
  `0currency` STRING,
  `0cust_grp4` STRING,
  `0postal_cd_payer` STRING,
  ck_rebates_pln DECIMAL(14,2),
  `0cust_grp3` STRING,
  rk_fc1_amount DECIMAL(17,2),
  yearmonth DATE,
  drespl1 STRING,
  ck_gross_sales_amt_act DECIMAL(17,2),
  `0billtoprty` STRING,
  `0curkey_tc` STRING,
  product_brand_cluster STRING,
  `0base_uom` STRING,
  customer_employee_cwid STRING,
  `0sapmaterial_code_trim` STRING,
  `0cs_dimen` STRING,
  dbrand___t STRING,
  dcustgrp2___t STRING,
  `0street_payer` STRING,
  `0country_payer` STRING,
  `0material___t` STRING,
  `0matl_type` STRING,
  `0fiscper` STRING,
  d_fgps STRING,
  dcustgrp5___t STRING,
  dprodh3___t STRING,
  `0base_uom___t` STRING,
  `0city_soldto` STRING,
  `0customer___t_billto` STRING,
  dprodh3 STRING,
  dctyspcod STRING,
  `0salesorg` STRING,
  d_gtin2 STRING,
  `0customer_billto_trim` STRING,
  d_pptyp STRING,
  product_portfolio_management STRING,
  `0street_soldto` STRING,
  d_prsite STRING,
  d_trian STRING,
  product_cnk_material_code STRING,
  rk_fc2_amount DECIMAL(17,2),
  d_xdisco STRING,
  dcustgrp4 STRING,
  dprodh2 STRING,
  `0customer_soldto` STRING,
  dprodh6___t STRING,
  `0city_shipto` STRING,
  ck_discnt_amt_pln DECIMAL(17,2),
  `0matl_grp_1` STRING,
  dru STRING,
  `0del_flag` STRING,
  `0postal_cd_soldto` STRING,
  date_text STRING,
  `0customer_soldto_trim` STRING,
  `0co_area_1` STRING,
  `0calmonth` STRING,
  division STRING,
  dprodh4___t STRING,
  `0pcompany` STRING,
  `0postal_cd_billto` STRING,
  organization STRING,
  rk_fc3_quantity DECIMAL(17,3),
  dan_cog___t STRING,
  `0customer_billto` STRING,
  `0prodh1` STRING,
  dctyspcod___t STRING,
  `0comp_code___t` STRING,
  dcustgrp3 STRING,
  dbrandf STRING,
  `0unit` STRING,
  d_lcpha STRING,
  `0country_2` STRING,
  `0customer___t_soldto` STRING,
  channel STRING,
  product_sub_brand STRING,
  `0eanupc` STRING,
  ck_not_assigned_pln DECIMAL(14,2),
  d_gtin1u STRING,
  d_gtin2c___t STRING,
  product_country_code STRING,
  `0division` STRING,
  `0prod_hier` STRING,
  date STRING,
  `0comp_code_2` STRING,
  `0customer_payer_trim` STRING,
  `0prodh6___t` STRING,
  `0gi_date` STRING,
  dbrandf___t STRING,
  ETLIsDeleted BOOLEAN,
  `0street_shipto` STRING,
  `0vtype` STRING,
  product_cnk_material_name STRING,
  ck_secondary_rebates_pln DECIMAL(17,2),
  dlp_form__t STRING,
  `0fiscvarnt` STRING,
  dctry_fd STRING,
  `0city_billto` STRING,
  `0customer` STRING,
  period_ytd_pytd INT)
USING delta
COMMENT 'The table \'fico_fact_copa_fin_legal\' in the \'emea_raw_lnd_bnlx\' schema contains financial and legal data related to key figures, countries, sales customers, plants, materials, sales organizations, and company codes. It serves as a central repository for cost and revenue information, allowing for analysis of sales performance, profitability, and compliance with legal requirements. The data in this table is crucial for financial reporting, decision-making, and ensuring regulatory compliance within the business.'
LOCATION 'abfss://curated@zpdatacoresadpemea.dfs.core.windows.net/discovery/bnlx/dwh_fact_ch_copa_sales_objectives_be_sql'
TBLPROPERTIES (
  'delta.deletedFileRetentionDuration' = 'interval 7 days',
  'delta.enableDeletionVectors' = 'true',
  'delta.feature.deletionVectors' = 'supported',
  'delta.logRetentionDuration' = 'interval 7 days',
  'delta.minReaderVersion' = '3',
  'delta.minWriterVersion' = '7')