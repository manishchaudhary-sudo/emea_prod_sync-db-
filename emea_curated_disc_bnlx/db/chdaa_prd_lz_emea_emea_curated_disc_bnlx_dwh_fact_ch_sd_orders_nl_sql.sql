CREATE TABLE chdaa_prd_lz_emea.emea_curated_disc_bnlx.dwh_fact_ch_sd_orders_nl_sql (
  ETLChecksum STRING,
  ETLLastModifiedDate TIMESTAMP,
  ETLIsDeleted BOOLEAN,
  d_pasite STRING,
  employee_cwid STRING,
  dprodh4 STRING,
  `0salesorg___t` STRING,
  sapsdohcredate DATE,
  `0country_shipto` STRING,
  `0dsdel_date` DATE,
  `0country___t_billto` STRING,
  dlp_form STRING,
  product_category STRING,
  `0country___t_soldto` STRING,
  `0lst_a_dd` DATE,
  d_raube STRING,
  `0soursystem` STRING,
  d_gtin1 STRING,
  `0country___t_payer` STRING,
  sapsdoicredate DATE,
  `0customer___t_shipto` STRING,
  `0customer_shipto_trim` STRING,
  dfksta STRING,
  cluster STRING,
  dcgrp STRING,
  product_iri_ean_code STRING,
  `0company` STRING,
  `0sched_line` STRING,
  employee_name STRING,
  `0customer___t_payer` STRING,
  `0customer_shipto` STRING,
  `0street_billto` STRING,
  product_iri_material_name STRING,
  product_brand STRING,
  dbrand STRING,
  `0reason_rej` STRING,
  dprodh2___t STRING,
  d_gtin2u___t STRING,
  dorqtybu DOUBLE,
  `0company___t` STRING,
  `0country_billto` STRING,
  d_fgpsu STRING,
  d_uvp STRING,
  d_gtin1c STRING,
  `0country_soldto` STRING,
  d_customer_mapping_code STRING,
  main_sap_customer_code_1 STRING,
  dprodh5 STRING,
  d_gtin2c STRING,
  `0postal_cd_shipto` STRING,
  sapsddocdate DATE,
  `0country___t_shipto` STRING,
  `0customer_payer` STRING,
  dprodh5___t STRING,
  `0country` STRING,
  `0city_payer` STRING,
  d_cpckd STRING,
  `0subtotal_1` DOUBLE,
  dprodh1___t STRING,
  status STRING,
  `0extmatlgrp` STRING,
  `0prodh6` STRING,
  d_gtin2u STRING,
  product_status STRING,
  `0dlv_sts` STRING,
  rls_auth_sid STRING,
  dan_cog STRING,
  dprodh1 STRING,
  `0matl_type___t` STRING,
  product_name STRING,
  `0matl_group` STRING,
  dprodh6 STRING,
  d_gtin1u___t STRING,
  d_raube___t STRING,
  `0s_ord_item` STRING,
  ETLLineageID STRING,
  d_cbpck STRING,
  `0doc_number` STRING,
  `0material` STRING,
  `0doc_type` STRING,
  `0plant` STRING,
  d_gtin1c___t STRING,
  `0ship_cond` STRING,
  daedato DATE,
  division___t STRING,
  main_sap_material_code STRING,
  `0gt_bsarksd` STRING,
  `0currency` STRING,
  `0doc_date` DATE,
  `0net_price` DOUBLE,
  `0postal_cd_payer` STRING,
  country_code STRING,
  `0billtoprty` STRING,
  product_brand_cluster STRING,
  `0base_uom` STRING,
  `0sapmaterial_code_trim` STRING,
  sap_apb_code STRING,
  `0ship_to` STRING,
  dbrand___t STRING,
  `0cml_cf_qty` DOUBLE,
  `0street_payer` STRING,
  `0country_payer` STRING,
  `0material___t` STRING,
  sapsdoichgdate DATE,
  `0matl_type` STRING,
  `0fiscper` STRING,
  d_fgps STRING,
  `0so_i_cnt` DOUBLE,
  dprodh3___t STRING,
  `0base_uom___t` STRING,
  `0city_soldto` STRING,
  `0customer___t_billto` STRING,
  `0sold_to` STRING,
  dprodh3 STRING,
  `0overall_st` STRING,
  `0salesorg` STRING,
  d_gtin2 STRING,
  `0customer_billto_trim` STRING,
  `0payer` STRING,
  d_pptyp STRING,
  product_portfolio_management STRING,
  derdatoh DATE,
  `0comp_code` STRING,
  `0street_soldto` STRING,
  d_prsite STRING,
  d_trian STRING,
  derdato DATE,
  product_cnk_material_code STRING,
  dprodh2 STRING,
  `0customer_soldto` STRING,
  dprodh6___t STRING,
  `0city_shipto` STRING,
  `0del_flag` STRING,
  `0postal_cd_soldto` STRING,
  `0customer_soldto_trim` STRING,
  division STRING,
  dprodh4___t STRING,
  sapsdreqdeldate DATE,
  `0postal_cd_billto` STRING,
  divestsna STRING,
  organization STRING,
  dan_cog___t STRING,
  `0customer_billto` STRING,
  dflsta STRING,
  `0comp_code___t` STRING,
  dbrandf STRING,
  d_lcpha STRING,
  `0customer___t_soldto` STRING,
  channel STRING,
  product_sub_brand STRING,
  `0logsys` STRING,
  dnetvallc DOUBLE,
  `0eanupc` STRING,
  d_gtin1u STRING,
  d_gtin2c___t STRING,
  product_country_code STRING,
  `0customer_payer_trim` STRING,
  `0prodh6___t` STRING,
  dbrandf___t STRING,
  `0net_value` DOUBLE,
  `0street_shipto` STRING,
  product_cnk_material_name STRING,
  dlp_form__t STRING,
  `0city_billto` STRING)
USING delta
LOCATION 'abfss://curated@zpdatacoresadpemea.dfs.core.windows.net/discovery/bnlx/dwh_fact_ch_sd_orders_nl_sql'
TBLPROPERTIES (
  'delta.deletedFileRetentionDuration' = 'interval 7 days',
  'delta.enableDeletionVectors' = 'true',
  'delta.feature.deletionVectors' = 'supported',
  'delta.logRetentionDuration' = 'interval 7 days',
  'delta.minReaderVersion' = '3',
  'delta.minWriterVersion' = '7')