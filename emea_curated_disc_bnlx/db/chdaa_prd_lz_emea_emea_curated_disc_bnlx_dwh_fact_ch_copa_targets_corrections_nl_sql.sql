CREATE TABLE chdaa_prd_lz_emea.emea_curated_disc_bnlx.dwh_fact_ch_copa_targets_corrections_nl_sql (
  ETLIsDeleted BOOLEAN,
  ETLChecksum STRING,
  ETLLineageID STRING,
  ETLLastModifiedDate TIMESTAMP,
  dsbe STRING,
  d_pasite STRING,
  `0cust_grp5` STRING,
  `0division_edit` STRING,
  drespo STRING,
  `0cust_group___t` STRING,
  `0fiscyear` STRING,
  `0price_grp` STRING,
  `0coorder` STRING,
  `0pstng_date` STRING,
  ck_secondary_rebates_pln DECIMAL(27,2),
  `0gl_account` STRING,
  dcustgrp3___t STRING,
  dprodh4 STRING,
  targets_discount_target_calc FLOAT,
  `0calquart1` STRING,
  `0distr_chan_edit` STRING,
  `0salesorg___t` STRING,
  `0country_shipto` STRING,
  date DATE,
  `0country___t_billto` STRING,
  dlp_form STRING,
  product_category STRING,
  `0country___t_soldto` STRING,
  copa_year DATE,
  ck_not_assigned_act DECIMAL(24,2),
  d_cs_grpa STRING,
  d_raube STRING,
  `0cust_grp1` STRING,
  ck_returns_act DECIMAL(24,2),
  ck_rebates_other_act DECIMAL(24,2),
  d_gtin1 STRING,
  rk_fc1_quantity DECIMAL(27,3),
  `0country___t_payer` STRING,
  `0customer___t_shipto` STRING,
  `0customer_shipto_trim` STRING,
  dcgrp STRING,
  product_iri_ean_code STRING,
  `0cust_grp2` STRING,
  dpc STRING,
  targets_objective_type STRING,
  `0company` STRING,
  `0curkey_lc` STRING,
  `0division___t` STRING,
  `0cs_chart` STRING,
  `0customer___t_payer` STRING,
  `0fiscper3` STRING,
  `0customer_shipto` STRING,
  `0street_billto` STRING,
  customer_country_code STRING,
  customer_status STRING,
  product_iri_material_name STRING,
  customer_main_sap_customer_code_1 STRING,
  customer_organization STRING,
  product_brand STRING,
  `0accnt_asgn_1` STRING,
  `0bill_type` STRING,
  dbrand STRING,
  rk_fc3_quantity DECIMAL(27,3),
  dprodh2___t STRING,
  `0cs_doctype` STRING,
  d_gtin2u___t STRING,
  `0company___t` STRING,
  dcustgrp1 STRING,
  `0country_billto` STRING,
  d_fgpsu STRING,
  d_uvp STRING,
  d_xnetsal STRING,
  d_gtin1c STRING,
  customer_employee_name STRING,
  `0country_soldto` STRING,
  dprodh5 STRING,
  `0calyear` STRING,
  d_gtin2c STRING,
  period_mtd_pmtd INT,
  `0postal_cd_shipto` STRING,
  dcustgrp1___t STRING,
  ck_secondary_rebates_act DECIMAL(27,2),
  `0version` STRING,
  ck_rebates_other_pln DECIMAL(24,2),
  `0country___t_shipto` STRING,
  `0cust_sales___t` STRING,
  `0customer_payer` STRING,
  dprodh5___t STRING,
  `0city_payer` STRING,
  d_cpckd STRING,
  dprodh1___t STRING,
  dcustgrp2 STRING,
  customer_sap_customer_name STRING,
  `0cust_group` STRING,
  `0extmatlgrp` STRING,
  ck_returns_pln DECIMAL(24,2),
  `0distr_chan___t` STRING,
  `0prodh6` STRING,
  customer_channel STRING,
  d_gtin2u STRING,
  product_status STRING,
  rls_auth_sid STRING,
  `0curkey_gc` STRING,
  `0distr_chan` STRING,
  ck_discnt_amt_act DECIMAL(27,2),
  ck_not_assigned_pln DECIMAL(24,2),
  dan_cog STRING,
  dprodh1 STRING,
  period_ytd_pytd_full_month INT,
  `0matl_type___t` STRING,
  ck_disc_pln DECIMAL(24,2),
  dvf STRING,
  product_name STRING,
  rk_quantity DECIMAL(27,3),
  `0matl_group` STRING,
  dprodh6 STRING,
  period_lfy_plfy INT,
  `0cust_sales` STRING,
  d_gtin1u___t STRING,
  d_raube___t STRING,
  d_xgross STRING,
  `0accnt_asgn` STRING,
  targets_gross_sales_target_calc FLOAT,
  ck_rebates_act DECIMAL(24,2),
  d_cbpck STRING,
  dcustgrp4___t STRING,
  `0material` STRING,
  `0doc_type` STRING,
  `0plant` STRING,
  d_gtin1c___t STRING,
  dcustgrp5 STRING,
  rk_fc3_amount DECIMAL(27,2),
  `0cs_item` STRING,
  targets_net_sales_target_calc FLOAT,
  division___t STRING,
  main_sap_material_code STRING,
  d_msrca STRING,
  dbg STRING,
  `0currency` STRING,
  ck_gross_sales_amt_act DECIMAL(27,2),
  `0cust_grp4` STRING,
  `0postal_cd_payer` STRING,
  `0cust_grp3` STRING,
  drespl1 STRING,
  `0billtoprty` STRING,
  `0curkey_tc` STRING,
  product_brand_cluster STRING,
  `0base_uom` STRING,
  customer_employee_cwid STRING,
  `0sapmaterial_code_trim` STRING,
  `0cs_dimen` STRING,
  copa_date DATE,
  dbrand___t STRING,
  dcustgrp2___t STRING,
  `0street_payer` STRING,
  customer_cluster STRING,
  rk_fc2_amount DECIMAL(27,2),
  `0country_payer` STRING,
  `0material___t` STRING,
  `0matl_type` STRING,
  `0fiscper` STRING,
  d_fgps STRING,
  ck_gross_qnty_pln DECIMAL(27,3),
  dcustgrp5___t STRING,
  dprodh3___t STRING,
  `0base_uom___t` STRING,
  `0city_soldto` STRING,
  `0customer___t_billto` STRING,
  ck_disc_act DECIMAL(24,2),
  dprodh3 STRING,
  ck_disc_cash_act DECIMAL(24,2),
  dctyspcod STRING,
  `0salesorg` STRING,
  d_gtin2 STRING,
  `0customer_billto_trim` STRING,
  d_pptyp STRING,
  product_portfolio_management STRING,
  ck_net_sales_amt_act DECIMAL(27,2),
  `0street_soldto` STRING,
  d_prsite STRING,
  d_trian STRING,
  product_cnk_material_code STRING,
  rk_fc2_quantity DECIMAL(27,3),
  d_xdisco STRING,
  dcustgrp4 STRING,
  dprodh2 STRING,
  ck_gross_sales_amt_pln DECIMAL(27,2),
  `0customer_soldto` STRING,
  dprodh6___t STRING,
  `0city_shipto` STRING,
  `0matl_grp_1` STRING,
  dru STRING,
  `0del_flag` STRING,
  rk_quantity_pln DECIMAL(27,3),
  `0postal_cd_soldto` STRING,
  date_text STRING,
  `0customer_soldto_trim` STRING,
  `0co_area_1` STRING,
  ck_discnt_amt_pln DECIMAL(27,2),
  rk_fc1_amount DECIMAL(27,2),
  `0calmonth` STRING,
  division STRING,
  dprodh4___t STRING,
  `0pcompany` STRING,
  `0postal_cd_billto` STRING,
  dan_cog___t STRING,
  `0customer_billto` STRING,
  ck_net_sales_amt_pln DECIMAL(27,2),
  dctyspcod___t STRING,
  `0comp_code___t` STRING,
  dcustgrp3 STRING,
  dbrandf STRING,
  `0unit` STRING,
  d_lcpha STRING,
  ck_gross_qnty_act DECIMAL(27,3),
  `0country_2` STRING,
  `0customer___t_soldto` STRING,
  product_sub_brand STRING,
  copa_year_month DATE,
  `0eanupc` STRING,
  d_gtin1u STRING,
  d_gtin2c___t STRING,
  product_country_code STRING,
  `0division` STRING,
  `0prod_hier` STRING,
  `0comp_code_2` STRING,
  `0customer_payer_trim` STRING,
  `0prodh6___t` STRING,
  `0gi_date` STRING,
  dbrandf___t STRING,
  `0street_shipto` STRING,
  `0vtype` STRING,
  product_cnk_material_name STRING,
  dlp_form__t STRING,
  `0fiscvarnt` STRING,
  ck_rebates_pln DECIMAL(24,2),
  ck_disc_cash_pln DECIMAL(24,2),
  dctry_fd STRING,
  `0city_billto` STRING,
  `0customer` STRING,
  period_ytd_pytd INT)
USING delta
LOCATION 'abfss://curated@zpdatacoresadpemea.dfs.core.windows.net/discovery/bnlx/dwh_fact_ch_copa_targets_corrections_nl_sql'
TBLPROPERTIES (
  'delta.deletedFileRetentionDuration' = 'interval 7 days',
  'delta.enableDeletionVectors' = 'true',
  'delta.feature.deletionVectors' = 'supported',
  'delta.logRetentionDuration' = 'interval 7 days',
  'delta.minReaderVersion' = '3',
  'delta.minWriterVersion' = '7')