CREATE TABLE chdaa_prd_lz_emea.emea_raw_lnd_ceec.esalesweb_activity (
  irda_sys_is_deleted BOOLEAN,
  signed_by_myself FLOAT,
  cancellation_reason STRING,
  sales_target_id2 STRING,
  irda_sys_deleted TIMESTAMP,
  follow_up_predecessor_id STRING,
  to_be_processed FLOAT,
  comment_id STRING,
  declaration STRING,
  sign_user_id STRING,
  event_max_members FLOAT,
  offline_changed_date TIMESTAMP,
  date_started TIMESTAMP,
  irda_sys_correlation_id STRING,
  irda_sys_changed_by STRING,
  sales_target_id STRING,
  gps_longitude FLOAT,
  sign_image_src STRING,
  team STRING,
  location_phone STRING,
  migration_no STRING,
  pim_mail_no STRING,
  alarm_unit STRING,
  is_customer_interaction FLOAT,
  is_visit FLOAT,
  irda_sys_object_guid STRING,
  offline_created_date TIMESTAMP,
  time_zone STRING,
  date_ended_tz TIMESTAMP,
  object_id DECIMAL(20,0),
  irda_sys_country STRING,
  irda_sys_updated TIMESTAMP,
  time_started TIMESTAMP,
  attribute_visibility_code STRING,
  description STRING,
  status STRING,
  gps_distance FLOAT,
  is_target_interaction FLOAT,
  goal_of_meeting STRING,
  userchanged TIMESTAMP,
  parent_activity_id STRING,
  gps_setdate TIMESTAMP,
  business_unit STRING,
  action_of_meeting STRING,
  zip_code_id STRING,
  irda_sys_touched TIMESTAMP,
  notify_comment STRING,
  template_activity_id STRING,
  subject STRING,
  target_entity STRING,
  is_compliant_visit FLOAT,
  follow_up_id STRING,
  activity_channel STRING,
  country_code STRING,
  order_taken FLOAT,
  priority FLOAT,
  sign_person_name STRING,
  activity_type_id STRING,
  activity_no STRING,
  year_started_tz STRING,
  time_ended TIMESTAMP,
  accompanied FLOAT,
  user_id STRING,
  organization_id STRING,
  custom_flag FLOAT,
  createdby STRING,
  is_template FLOAT,
  sys_source_system STRING,
  changedby STRING,
  customer_id STRING,
  email STRING,
  is_edetailing_completed FLOAT,
  result_of_meeting STRING,
  pim_cal_no STRING,
  date_started_tz TIMESTAMP,
  irda_sys_created TIMESTAMP,
  district STRING,
  client_variant_id DECIMAL(20,0),
  division STRING,
  additional_meta_data STRING,
  irda_sys_is_dirty BOOLEAN,
  region STRING,
  is_planned FLOAT,
  sign_date TIMESTAMP,
  activity_id STRING,
  alarm_offset FLOAT,
  location STRING,
  changed TIMESTAMP,
  irda_sys_unique_key STRING,
  deleted FLOAT,
  gps_latitude FLOAT,
  date_ended TIMESTAMP,
  created TIMESTAMP,
  month_started_tz STRING,
  alarm FLOAT,
  signature_id STRING,
  ETLIsDeleted BOOLEAN,
  ETLLastModifiedDate TIMESTAMP,
  ETLChecksum STRING,
  ETLLineageID STRING)
USING delta
LOCATION 'abfss://raw@zpdatacoresadpemea.dfs.core.windows.net/landing/ceec/esalesweb_activity'
TBLPROPERTIES (
  'delta.deletedFileRetentionDuration' = 'interval 7 days',
  'delta.logRetentionDuration' = 'interval 7 days',
  'delta.minReaderVersion' = '1',
  'delta.minWriterVersion' = '2')